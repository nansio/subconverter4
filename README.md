# Linkform

一个纯前端的 Clash Meta 代理项转换器。直接用浏览器打开 `index.html`，粘贴：

- `vless://`（Reality / Vision、TCP、WS、gRPC）
- `hysteria2://` 或 `hy2://`（含 salamander 混淆）

即可获得可复制或下载的 Clash Meta YAML `proxies` 片段。

链接不会发送到任何服务端。
